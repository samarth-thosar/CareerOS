"""DTOs that cross from the application layer into presentation.

Empty in this phase -- populated as each feature phase adds API endpoints (Phase 4 onward). Response
shapes belong here, not raw domain entities, so the REST contract never leaks domain internals.
"""
